# Example product-facing fields only. Advanced host internals inherit defaults.

[host]
# Default is loopback. Non-loopback requires tls or remote auth.
listen = "127.0.0.1:8080"
mode = "embedded" # or "standalone"
safe_mode = false

# standalone only — ServiceHost Link control app id
# link_endpoint = "local://mutsuki.servicehost"

# Optional local auth token reference (never commit real secrets).
# auth_token = "secret:webhost.auth_token"
